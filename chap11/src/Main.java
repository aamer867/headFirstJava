//TIP To <b>Run</b> code, press <shortcut actionId="Run"/> or
// click the <icon src="AllIcons.Actions.Execute"/> icon in the gutter.
public class Main {
    public static void main(String[] args) {
        /*
        * Generics:
        *   Add more layer of type safety, like when we have this issue:
        *       Add an object to an ArrayList, before generics, we can add any object of type Object only,
        *       and returning it as Object also, so if I added a cat to an ArraList, I will also be able to
        *       add any other Type to it, like a dog, or even a fruit. But with generics, we are able to add
        *       another safety layer like, we can make sure that the input and output objects in the ArrayList
        *       are from the same type.
        *   Type Parameter:
        *       It is the type, we see in the collection class like T or E, where it means that the complier
        *       will replace with the new given type. The convention to write it with the single letter T,
        *       but if we are going to write our own Generic class, we can use E, or R.
        * */
    }
}